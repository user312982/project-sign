# ============================================
# ASL Recognition API - TFLite Version
# ============================================
# Lightweight dependencies for Railway free tier

# Core Web Framework
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# TFLite Runtime (lightweight, try in order)
# Note: Dockerfile handles TFLite runtime installation with fallback

# Data Processing
numpy>=1.24.0
pillow>=9.5.0

# Note: tensorflow is NOT required for production
# The Dockerfile installs ai-edge-litert or tensorflow-cpu as fallback
